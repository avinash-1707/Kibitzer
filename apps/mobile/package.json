{
  "name": "@kibitzer/mobile",
  "version": "0.0.0",
  "private": true,
  "main": "expo-router/entry",
  "scripts": {
    "start": "expo start --dev-client",
    "ios": "expo run:ios",
    "android": "expo run:android"
  },
  "//": "Unit 0 skeleton. Units F/G/H add real deps via `npx expo install` (expo, expo-router, react-native, react-native-sse, expo-audio, expo-camera, expo-secure-store) and fill the screens.",
  "dependencies": {
    "@kibitzer/shared": "workspace:*"
  },
  "devDependencies": {}
}
