// STUB — owned by Unit F (pairing screen). Fill: QR scan / paste → validate → store base → route.
import { Text, View } from "react-native";

export default function PairScreen() {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text>Pairing — not implemented (Unit F)</Text>
    </View>
  );
}
